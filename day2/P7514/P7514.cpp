#include <bits/stdc++.h>
using namespace std;
#define int long long

const int MAX_N = 1e6+6;
int n,m,a[MAX_N],b[MAX_N];

signed main(){
    cin>>n>>m;
    for (int i=1; i<=n; i++) {
        cin>>a[i];
    }
    for (int i=1; i<=n; i++) {
        cin>>b[i];
    }
    
}